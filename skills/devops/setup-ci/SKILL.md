---
name: setup-ci
description: Scaffolds GitHub Actions CI pipeline for Go or TypeScript/Bun projects.
---

> ⚠️ DEFERRED — tulis saat ada proyek konkret yang butuh CI setup.

Intent: Generate `.github/workflows/ci.yml` dengan lint, test, build steps. Support Go (`go test`, `golangci-lint`) dan TypeScript/Bun (`bun test`, `bun run typecheck`).
